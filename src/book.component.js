import React,{Component} from 'react'
import PropTypes  from 'prop-types' 

export default class Book extends Component{

  constructor(props){
    super(props);
    this.state={};
  }
  // const mapDispatchToProps = dispatch => {
  //   return {
  //     // create: (info) => dispatch(action.create(info)), // send event
  //     // update: (info) => dispatch(action.update(info)), // send event
  //     // load: (info) => dispatch(action.load(info)), // send event
  //   }
  // }




}